// Locales
export type IActiveLocale = {
    label: string;
    code: string
    flagUrl: string;
}

// Breadcrumbs
export type IBreadCrumb = {
    label: string;
    path: string;
}

//Badges
export type IBadge = {
    label: string;
    color: string;
    bgColor: string;
}